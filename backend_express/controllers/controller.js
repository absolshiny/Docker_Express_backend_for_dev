// Handles logic for user-related API endpoints (e.g., sign-up, login, profile retrieval).
// Create as many files as needed  remember to keep it encapsulated
// I would sayt that this is in fact required at minimun if you want to create logic for your program


exports.helloWorld = (req, res) => {
    res.send('Hello World!');
  };